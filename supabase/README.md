# Supabase Notes

目前請以這份設定指南為主：

- [SETUP_RENDER_AND_AUTH.md](/C:/Users/insta/Desktop/interview_prep/supabase/SETUP_RENDER_AND_AUTH.md)

重點：

1. 在 `SQL Editor` 執行 [app_schema.sql](/C:/Users/insta/Desktop/interview_prep/supabase/app_schema.sql)
2. 到 `Authentication -> Sign In / Providers -> Email`
3. 確認：
   - `Allow new users to sign up` 開啟
   - `Confirm Email` 關閉

這樣這個專案的 `username + password` 登入模式才會正常運作。
